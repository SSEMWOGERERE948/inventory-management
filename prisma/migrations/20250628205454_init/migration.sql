BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[OrderRequest] ADD [approvedAt] DATETIME2,
[fulfilledAt] DATETIME2,
[rejectedAt] DATETIME2,
[shippedAt] DATETIME2;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
